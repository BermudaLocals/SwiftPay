{
  "name": "swiftpay-monolith",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "stripe": "^14.0.0"
  }
}
